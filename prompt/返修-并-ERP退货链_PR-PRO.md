# 返修指令 — ERP 退货链（采购退货单 PR + 采购退货出库单 PRO）· 可并行

> `返修-并-` = 可与其它返修并行跑。在 `forge-erp` 项目根用反重力/Codex。

```
Context:
我在返修 Forge 进销存(forge-erp)采购退货链已生成的 PRD。六维走查发现两个高阻断 + 若干中低问题。决策已定:一期【不支持分批退货出库】,PR:PRO = 严格 1:1。
先读(权威基线):
- context/05-单据分层与编码规则(只有"订单层/执行层"两层,明确"不等于三层单据";采购退货出库单应归第2层业务执行层,与采购入库单 PI 同级)、06-核心业务场景与流程、07-本期项目边界与业务约束、08-术语与口径字典
- 参照物套件:prd-docs/采购管理/采购入库单/(执行类最全8件)、采购订单/(单据类5件)
待修文件:prd-docs/采购管理/采购退货单/ 全部8个 + prd-docs/采购管理/采购退货出库单/ 全部8个

Request:
1. 【核心】删除 PRO 全套的分批出库设计:去掉"一张PR可生成多张PRO(1:N)""已出库/可出库数量"字段、用例里"分批出6剩2"等;改为严格 1:1(一张已确认 PR 下推唯一一张 PRO)。规则落到 PR 与 PRO 的业务规则规格,并写明"一期不支持分批退货出库"。
2. 单据分层修正:采购退货出库单主PRD §3 把"第3层-退货执行层"→改为"第2层·业务执行层,与采购入库单PI同级"(按 context/05 两层架构);采购退货单定位对齐,不自造第3层 taxonomy。
3. 权威层级头修正:8 份非Demo文档头部若写着"{单据}主PRD > context/ > templates/ > 本文件",一律改回"context/ > templates/ > prd-docs";撤销任何"以主PRD为准覆盖 context/05"的处理,回溯 context。
4. R05 悬空引用:采购退货单主PRD 的 R05 引用了不存在的"PRO取消流程"→改为明确一期纠错路径(如"确认后不可逆,纠错走线下/二期")。
5. Mock 数字统一:贯穿 PI→PR→PRO 用同一套数字,至少保证 PRO 用例与 PRO 流程推演一致。
6. Demo 重命名:采购退货出库单_Demo_执行页.md → 采购退货出库单_Demo_执行出库页.md(对齐 PI 的"执行入库页"命名)。
7. 路由参数风格统一:PR 用 sourcePiId(camelCase)、PRO 用 source_id(snake_case)→ 统一成一种。

Output format:
逐文件小改,与参照物 采购入库单/采购订单 套件的章节结构、表格、术语对齐。

红线禁止:
- 不改已有:不改 context/、templates/、参照物套件(采购入库单/采购订单)
- 不加不该有:执行层 PRO 状态只 草稿→已确认(+可选已作废),无审核流;不做无单退货出库、不做物流跟踪
- 不越界:不自创单号(PR/PRO/FL 前缀按 context/05);术语按 context/08;PR 确认不动账、仅 PRO 确认出库才 扣现存/冲应付/生FL,草稿不动账;Mock 2026;不编造
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完 PR+PRO 共16文件返修再汇报,列出每处改动。
```
