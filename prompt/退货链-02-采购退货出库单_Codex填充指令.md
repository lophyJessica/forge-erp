# Codex 填充指令 — ERP 采购退货出库单（PRO）

> 用法：在 `forge-erp` 项目根启动 Codex，粘贴以下五段式指令。主PRD 已存在，本指令只补其余 7 个文件。与采购退货单指令错开跑。

```
Context:
我在做 Forge 进销存(forge-erp)的 PRD 套件补全。读者是研发/测试/产品复核。
本次补全「采购退货出库单(PRO)」——采购退货链路的执行层单据,基于已确认的采购退货单(PR)下推,确认出库时扣减仓库现存、冲减供应商应付、生成库存流水(FL)。
先读:
- context/:05-单据分层与编码规则、06-核心业务场景与流程、07-本期项目边界与业务约束、08-术语与口径字典、04-主要功能模块和清单
- templates/:字段清单模板、状态机设计模板、单据状态与流转设计规范、业务流程推演模板、用例数据推演模板、Demo PRD模板
- 参照物(执行类 gold standard,结构/规则/流程/用例全照它):prd-docs/采购管理/采购入库单/ 整套 8 件
- 本单锚点:prd-docs/采购管理/采购退货出库单/采购退货出库单主PRD.md(V1.0,唯一事实来源)

Request:
补齐 prd-docs/采购管理/采购退货出库单/ 下 7 个文件(主PRD 不动):
字段清单、_业务规则规格、_业务流程推演、_用例数据推演、_Demo_列表页、_Demo_详情页,以及把占位的 _Demo_执行页.md 作为「执行出库页」填写(可重命名为 采购退货出库单_Demo_执行出库页.md,对齐参照物的「执行入库页」命名)。

Output format:
同采购入库单套件的章节结构与表格格式逐项对齐,只把「入库/收货」语义换成「退货出库」。

红线禁止:
- 不改已有:不动本单主PRD;不改 context/、templates/、参照物
- 不加不该有:执行层状态只「草稿→已确认」(+可选已作废),严禁「待审核/已审核」;无单退货出库是 Out of Scope,不要做;不加物流跟踪
- 不越界:确认出库才触发 扣现存/冲应付/生FL,草稿态不动账;术语按 context/08;单号 PRO/FL 前缀须在 context/05 有定义,没有就标注;Mock 用 2026;不编造数据
如信息不足,标注不确定性。

Checkpoint:
除非不可逆/范围变化/缺信息,否则做完再汇报。
```
