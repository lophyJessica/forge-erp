# Codex 填充指令 — ERP 采购退货单（PR）

> 用法：在 `forge-erp` 项目根启动 Codex，粘贴以下五段式指令。主PRD 已存在，本指令只补其余 7 个文件。与退货出库单指令错开跑。

```
Context:
我在做 Forge 进销存(forge-erp)的 PRD 套件补全。目标读者是研发/测试/产品复核。
本次补全「采购退货单(PR)」——它是采购退货链路的发起层单据,基于已确认的采购入库单(PI)下推创建。
两依赖已就位,请先读:
- 业务地基 context/:04-主要功能模块和清单、05-单据分层与编码规则、06-核心业务场景与流程、07-本期项目边界与业务约束、08-术语与口径字典、03-进销存上下游关联系统
- 格式模板 templates/:字段清单模板、状态机设计模板、单据状态与流转设计规范、业务流程推演模板、用例数据推演模板、Demo PRD模板
- 参照物套件(结构对齐):单据类结构照 prd-docs/采购管理/采购订单/ 的三页 Demo;规则/流程/用例三件的写法照最全的 prd-docs/采购管理/采购入库单/
- 本单锚点:prd-docs/采购管理/采购退货单/采购退货单主PRD.md(已是 V1.0,作为唯一事实来源)

Request:
基于上述输入,补齐 prd-docs/采购管理/采购退货单/ 下这 7 个文件(主PRD 已存在,不动):
采购退货单字段清单.md、采购退货单_业务规则规格.md、采购退货单_业务流程推演.md、采购退货单_用例数据推演.md、采购退货单_Demo_列表页.md、采购退货单_Demo_新增编辑页.md、采购退货单_Demo_详情页.md

Output format:
- 字段清单 = SSOT,分「基础/明细/系统」字段,标注来源(继承PI/用户填/系统算)、类型、必填、枚举
- 业务规则规格 = 状态机(草稿→已确认→…)+ 校验 + 权限 + 计算规则,唯一定义
- 业务流程推演 = 步骤 + 真实示例数据的动态沙盘
- 用例数据推演 = 带数字的公式/状态快照/走查清单
- 3 个 Demo = 页面结构/控件/交互/Mock,不重复定义字段枚举
- 每个占位文件顶部有「状态/角色/参照套件」提示头,写作前删掉那两行 HTML 注释

红线禁止:
- 不改已有:不动 采购退货单主PRD.md;不改 context/、templates/、参照物套件
- 不加不该有:PR 是发起层,确认后不直接扣减库存(库存在下游 PRO 执行时才动);不给执行层加多余审核流
- 不越界发挥:字段/单号前缀若 context/05 未定义,先标不确定、别自创;术语严格用 context/08(现存/占用/可用);Mock 日期用 2026;数据冲突以既有主PRD为准,不编造
如信息不足,明确标注不确定性,不要脑补。

Checkpoint:
除非涉及不可逆操作、范围变化或需要我补信息,否则一次做完 7 个文件再汇报,附一句每个文件写了啥。
```
